1. all edits to crop database shoudl be saved locally and and not to server every edit to crop database, fertilize database, should be save locally and persist locally, and copy of each of the db should should live locally when the PWA is installed on a device.
2. if a estimated transplant day for a crop has pass the that crop thansplant date must be readjust, similar to the crop db readjustment  function in `c:\Users\ALEX\Desktop\Crop-Manager\file\CropManager_FULL_v9_POLLING (1).gs` we will update the local db to have a readjustment, also the  progresion tracker that show the current stste of a crop if its Planted, Germinated, Transplanted, or Harvested i want the ability to manually tick or untick each progression which will be wired to the crop "update status button.
3. i want to make sure all the dates filled out in Calendar and reminder and cropcard(continuous harvest etc.) are calculated using the actual time frame in `c:\Users\ALEX\Desktop\Crop-Manager\artifacts\cropmanager-pwa\public\data\crop_database.json` and `c:\Users\ALEX\Desktop\Crop-Manager\artifacts\cropmanager-pwa\public\data\fertilizer_schedule.json` and not use any preset dates in the app, all dates and schedule must be calculated by the app.
4. add a history button and history window, this will store time frame for crops and show the average timeframe between stage, log will have the date from planting to germination, then from germination to transplant, and from transplant to harvest, so the necessary data will be collected for each crop, to determine the time frame between each stage, also propagation and corp will share this area.
5. propagation will learn from crop, since crop is already being planted fron seeds propagation will in heretrit these data
6. after implimenting run type check for errors and test the app for build errors then push to github main